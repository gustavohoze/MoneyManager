import Foundation

enum CloudKitConstants {
    // Keep one source of truth for CloudKit container configuration.
    static let containerIdentifier = "iCloud.shecraa.MoneyManager"

    // Enable sync once the target entitlements and CloudKit container are configured.
    static let isSyncEnabled = true
}
